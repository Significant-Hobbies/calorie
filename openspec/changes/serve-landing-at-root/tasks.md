# Tasks

- [x] 1. Snapshot `ios-landings` Calorie dist into `marketing/`
- [x] 2. Build the journal to `dist/app` with base `/app/`
- [x] 3. Worker rewrite for `/app/*`; PWA and auth follow
- [x] 4. `pnpm typecheck` and `pnpm test` plus `pnpm build`
